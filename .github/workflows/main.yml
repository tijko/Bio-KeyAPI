name: Run tests on any Push event
on:
  push:
    branches:
      - '*'
jobs:
  run_tests:
    runs-on: [ubuntu-latest]

    steps:
      - uses: actions/checkout@v2
      - name: Docker-Deploy 
        run: | # Going to remove this for testing
            docker-compose -f docker-compose.yml up --build -d
            docker-compose up &
      - name: Run tests
        run: pytest tests/Bio-KeyAPI-test.py